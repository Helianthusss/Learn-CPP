#include<bits/stdc++.h>
using namespace std;

int main(){
    int candles,makeNew;
    cin>>candles>>makeNew;
    if(candles > makeNew) {
        while(thuong<)
        float hieu=(float)candles/makeNew;
        float ans=round(hieu);
        cout<<pow(ans,2);
    }
    else if(candles = makeNew) cout<<candles+1;
    else cout<<candles;
    return 0;
}